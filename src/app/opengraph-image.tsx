// Generated by Eazo deploy share metadata. Safe to overwrite.
import { ImageResponse } from 'next/og';

export const alt = "育见·我的育儿记录 app preview";
export const size = { width: 1200, height: 630 };
export const contentType = 'image/png';

const appName = "育见·我的育儿记录";
const description = "养育孩子没有标准答案，但你不必独自摸索。育见帮你记录孩子从出生起的每一个变化和每一次惊喜，也在你感到困惑时，给你有科学依据、不带评判的回应。从建立成长档案开始，温和引导你一步步往前走。你的数据完全属于你，我们只是静静陪伴。";
const icon = "💡";

function isImageIcon(value: string) {
  return value.startsWith('data:image/') || value.startsWith('http://') || value.startsWith('https://');
}

export default function Image() {
  const fallbackGlyph = appName.trim().slice(0, 1).toUpperCase() || 'E';

  return new ImageResponse(
    (
      <div
        style={{
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'space-between',
          background: '#F7F4EE',
          color: '#1D1B18',
          padding: '76px',
          fontFamily: 'Inter, Arial, sans-serif',
        }}
      >
        <div style={{ display: 'flex', alignItems: 'center' }}>
          <div
            style={{
              width: 168,
              height: 168,
              borderRadius: 40,
              background: '#FFFFFF',
              border: '1px solid #E4DED4',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              overflow: 'hidden',
              marginRight: 44,
            }}
          >
            {isImageIcon(icon) ? (
              <img
                src={icon}
                alt=""
                width="168"
                height="168"
                style={{ width: 168, height: 168 }}
              />
            ) : (
              <div style={{ fontSize: 84, fontWeight: 800, lineHeight: 1 }}>
                {icon || fallbackGlyph}
              </div>
            )}
          </div>
          <div style={{ display: 'flex', flexDirection: 'column', flex: 1 }}>
            <div style={{ fontSize: 72, fontWeight: 800, lineHeight: 1.05, letterSpacing: 0 }}>
              {appName}
            </div>
            <div
              style={{
                fontSize: 32,
                lineHeight: 1.35,
                color: '#5F5A52',
                marginTop: 24,
                maxWidth: 760,
              }}
            >
              {description}
            </div>
          </div>
        </div>
        <div style={{ display: 'flex', alignItems: 'center', fontSize: 28, color: '#8A8176' }}>
          <div
            style={{
              width: 18,
              height: 18,
              borderRadius: 999,
              background: '#FF6B2C',
              marginRight: 14,
            }}
          />
          Built with Eazo
        </div>
      </div>
    ),
    size,
  );
}
