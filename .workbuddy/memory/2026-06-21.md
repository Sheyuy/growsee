# 2026-06-21 开发日志

## 完成工作

### 1. 环境变量配置 (.env.example)
- 创建了完整的 `.env.example` 文件
- 包含 PostgreSQL、MiniMax AI、本地文件存储、微信小程序、JWT 等所有配置项
- 添加了详细的注释说明，每个字段都有获取/填写指南
- 将腾讯云 COS 改为可选配置（注释状态），默认使用本地存储

### 2. 本地文件存储 API
- 重写了 `/api/upload/photo` 路由
- 支持 multipart/form-data 文件上传
- 文件保存在 `public/uploads/` 下，通过 HTTP 直接访问
- 添加了文件大小限制（5MB）和类型校验（仅图片）
- 生成唯一文件名，按用户 ID 分目录存储

### 3. Demo 登录模式
- 创建了 `/api/auth/demo/login` 路由
- 无需微信小程序，自动创建模拟用户并签发 JWT
- 支持 POST（自定义用户名）和 GET（默认用户）两种方式
- 用户数据写入真实数据库，体验完全一致

### 4. 客户端认证 Hook (useAuth)
- 重写了 `useAuth` hook，支持双模式：
  - Demo 模式：自动登录，无需交互
  - 微信模式：标准 OAuth 流程（保留但暂未启用）
- 添加了 `isDemo` 状态标识
- 支持自动 session 检测、登录、登出

### 5. 部署文档 (DEPLOY.md)
- 编写了完整的部署指南
- 包含 3 种对外展示方案对比（ngrok 穿透 / 服务器部署 / Vercel）
- 数据库方案对比（本地 Docker / 免费托管 / 自有服务器）
- 详细的步骤命令和常见问题解答

### 6. .gitignore 更新
- 添加 `public/uploads/` 到 gitignore，防止上传文件被提交

## 待办
- 用户需要填写 `.env` 文件并启动数据库
- 运行 `npm run db:migrate` 执行数据库迁移
- 可选：运行 `npx tsx scripts/seed-test-data.ts` 填充演示数据

## 决策记录
- 文件存储：选择本地文件系统（零成本），后续可无缝切换到 R2/七牛云
- 数据库：优先推荐本地 Docker 或 Supabase/Neon 免费托管
- Demo 展示：推荐 ngrok 内网穿透（5 分钟搞定）或部署到 tc-ts 服务器
- 微信小程序：Demo 阶段跳过，用 Demo 模式替代

## 2026-06-21 15:30 - 纯前端化重构（重大变更）

### 需求变更
用户决定暂时不做小程序、数据库和微信登录，仅作为纯前端 Demo 使用，示范数据放在 GitHub 上。

### 所做工作
1. **创建示范数据 JSON**: 5 个文件在 `public/data/`：
   - `demo-children.json` — 2 个孩子（小雨/小阳）
   - `demo-records.json` — 8 条成长记录（覆盖五维分类）
   - `demo-letters.json` — 4 封悄悄话/时间胶囊
   - `demo-mood.json` — 5 条父母情绪记录
   - `demo-milestones.json` — 4 个里程碑

2. **创建模拟数据层** (`src/lib/demo/store.ts`): 
   - LocalStorage 持久化存储
   - 首次加载从 JSON 初始化数据
   - 提供所有 CRUD 操作
   - 支持重置功能

3. **创建纯前端 AI 引擎** (`src/lib/demo/ai-engine.ts`):
   - 基于关键词匹配的预设回复系统
   - 覆盖 10 个常见育儿话题（语言、情绪、分离焦虑、阅读、死亡话题、手足关系、乐高、牙齿、生日等）
   - 流式打字效果模拟
   - 兜底回复处理未知问题

4. **重写所有组件**:
   - HomeScreen / RecordScreen / AiCompanionScreen / MoodScreen / HeartLettersScreen / TimelineMilestonesScreen / UserProfileScreen / ScientificInsightScreen / QuickNoteSheet / QuickSetupGuide / RecordEditorModal
   - 全部改为从 demo store 获取数据
   - 认证 hook 改为完全本地模拟

5. **清理后端代码**:
   - 删除 `src/app/api/` 目录（所有 API 路由）
   - 删除 `src/lib/db/` 目录（数据库 schema + queries）
   - 删除 `src/lib/ai/` 目录（MiniMax 调用）
   - 删除 `src/lib/auth/` 目录（认证逻辑）
   - 删除 `src/lib/mcp/` 目录（MCP 服务器）
   - 删除 `src/lib/api/` 目录（HTTP 封装）
   - 删除 `scripts/` 目录

6. **创建类型定义** (`src/types/index.ts`): 从原来的 drizzle schema 提取纯类型定义

7. **配置静态导出** (`next.config.ts`):
   - `output: "export"`, `distDir: "dist"`
   - 移除 `opengraph-image.tsx` 和 `twitter-image.tsx`（动态路由不支持静态导出）

8. **构建成功**:
   - 9 个路由全部静态渲染成功
   - 生成 10 个 HTML 文件 + 5 个 demo JSON 文件
   - 总计 16M 输出，部署到 GitHub Pages 即可

9. **更新 README**: 提供 GitHub Pages 部署指南（3 种方案）

### 部署方式
用户可以直接把 `dist/` 目录内容推到 GitHub Pages，或者使用 GitHub Actions 自动构建部署。